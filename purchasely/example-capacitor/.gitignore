.DS_Store

# Generated by package manager
node_modules/

# The web assets are shared with the Cordova example (capacitor.config.json webDir),
# so both hosts run byte-identical app code. Nothing to ignore here.

